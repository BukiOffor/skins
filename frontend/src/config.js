const config = {
    basename: '',
    defaultPath: '/',
    borderRadius: 12,
    fontFamily: `'Poppins', sans-serif`,
    proxy: 'https://wolmart-backend.onrender.com/api/v1'
};

export default config;
